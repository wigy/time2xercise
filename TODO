[ ] Quit button to reset clock and go admin.
[ ] Check if angular audio can be replaced by ng-audio (or something better).
[ ] Back to beginning or to the end buttons.
[ ] Language selector.
[ ] Random shuffle option.
[ ] Use require() mechanism.
[ ] Clean up index.html to the smaller directives so that there is eventually only one left.
[ ] Deploy in Heroku.
[ ] Relase script turning debug off and updating version. (See grunt-copy and process option).
[ ] Way to sync package.json and VERSION in settings.js. (Require package.json itself in html?)
[ ] Template builder, which collects html files and converts them to javascript strings.
[ ] Study how to implement (most of) CSS as Bootstrap theme add-on.
[ ] Extract Grunt custom tasks to separate files.
